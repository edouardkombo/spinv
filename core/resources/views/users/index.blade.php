@extends('app')@section('content')<div class="col-md-12 content-header" >    <h1><i class="fa fa-user"></i> System users</h1></div><section class="content"><div class="row">    <div class="col-md-12">        @if (Session::has('flash_notification.message'))        {!! message() !!}        @endif        <div class="box box-primary">            <div class="box-header">                <div class="box-tools pull-right">                    <a href="{{ route('users.create') }}" class="btn btn-info btn-sm pull-right" data-toggle="ajax-modal"> <i class="fa fa-user-plus"></i> New User</a>                </div>            </div>            <div class="box-body">                    <table class="table table-bordered table-striped table-hover datatable">                        <thead>                        <tr>                            <th></th>                            <th>Photo </th>                            <th>Name </th>                            <th>Username </th>                            <th>Email </th>                            <th>Phone </th>                            <th>Action </th>                        </tr>                        </thead>                        <tbody>                        @foreach($users as $user)                        <tr>                            <td></td>                            <td><img src="{{ asset($user->photo != '' ? 'assets/img/uploads/'.$user->photo : 'assets/img/uploads/no-image.jpg' ) }}" class="listthumb img-circle" width="60px"/></td>                            <td>{{ $user->name }} </td>                            <td>{{ $user->username }} </td>                            <td>{{ $user->email }} </td>                            <td>{{ $user->phone }} </td>                            <td>                                {!! edit_btn('users.edit', $user->id) !!}                                @if(Auth::user()->id != $user->id)                                {!! delete_btn('users.destroy', $user->id) !!}                                @endif                            </td>                        </tr>                        @endforeach                        </tbody>                    </table>            </div>        </div>        <!-- End  Hover Rows  -->    </div></div></section>@endsection